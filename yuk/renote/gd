싱글 스레드에서는 항상 arraylist가 최고
아니면 concurrent큐 사용

hashmap은 싱글에서 최상 스필드
아니면 concurrent map 사용

for문 보다 foreach 보다
iter이 더 빠르다.=> iter은 중간삭제도 지원한다.

단일 변수에 대한 멀티 스레딩 접근은 아토믹 클래스를 이용한다.

collection 객체에서 addall 함수가
생성자에 추가하는것보다 훨씬 빠르다.
생성자 추가 방식은 1000개 이하일때 비슷한 스피드를 낸다.

connectior에서 어떠한 함수를 호출하든 stateChange 함수가 동작한다.

h2에서 meta object 사용하면 안됨